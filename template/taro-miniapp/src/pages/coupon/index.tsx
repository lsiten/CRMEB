import { useEffect, useState } from 'react'; import { Text, View } from '@tarojs/components'; import { getCoupons, type Coupon } from '../../services/assets'; import './index.scss';
export default function CouponPage() { const [coupons, setCoupons] = useState<readonly Coupon[]>([]); useEffect(() => { void getCoupons().then(setCoupons).catch(() => setCoupons([])); }, []); return <View className='page'><Text className='title'>我的优惠券</Text>{coupons.length ? coupons.map((coupon) => <View className='card coupon' key={coupon.id}><Text className='couponAmount'>¥{coupon.amount.toFixed(2)}</Text><View><Text>{coupon.title}</Text><Text className='hint'>满 ¥{coupon.minPrice.toFixed(2)} 可用 · {coupon.status}</Text></View></View>) : <View className='card empty'><Text>暂无优惠券</Text></View>}</View>; }
