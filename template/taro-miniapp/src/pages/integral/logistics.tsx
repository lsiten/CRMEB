import { useEffect, useState } from 'react'; import { Text, View } from '@tarojs/components'; import { useRouter } from '@tarojs/taro'; import { getIntegralLogistics } from '../../services/integral'; import './index.scss';
const LogisticsPage = () => { const id = useRouter().params['orderId'] ?? ''; const [items, setItems] = useState<readonly Readonly<{ time: string; description: string }>[]>([]); useEffect(() => { if (id) void getIntegralLogistics(id).then(setItems).catch(() => setItems([])); }, [id]); return <View className='page integralPage'><Text className='title'>物流跟踪</Text>{items.length ? items.map((item, index) => <View className='card bill' key={`${item.time}-${index}`}><Text>{item.description}</Text><Text className='hint'>{item.time}</Text></View>) : <View className='empty'><Text>暂无物流信息</Text></View>}</View>; }; export default LogisticsPage;
